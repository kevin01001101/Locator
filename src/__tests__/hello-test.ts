
describe('subtest', () => {
    it('is always true', () => {
        const trueVar = true;
        expect(trueVar).toBe(true);
    });
});



describe('another test', () => {
    it('is always false', () => {
        const trueVar = false;
        expect(trueVar).toBe(true);
    });
});
